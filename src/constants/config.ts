
/**
 * Маршруты
 */

export enum routes {
    home = "/",
    palette = "/palette",
}

/**
 * @defaultColor цвет который устанавливается по умолчанию в colorPiker
 */

export const defaultColor = {
    r: 129, g: 36, b: 151, a: 0.9
}